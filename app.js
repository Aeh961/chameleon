import React from 'react';
import ContactForm from './ContactForm'; // Adjust the path as needed

const App = () => {
    return (
        <div>
            {/* Other components like Header, Services, etc. */}
            <ContactForm />
        </div>
    );
};

export default App;
